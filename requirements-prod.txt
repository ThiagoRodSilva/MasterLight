# Dependências de runtime para produção (Hostinger compartilhada / Passenger).
# Alinhado com requirements.txt — usa PyMySQL como driver MySQL drop-in
# (mysqlclient exige build tools indisponíveis no shared).
Django==5.0.7
gunicorn==23.0.0
django-environ==0.11.2
python-decouple==3.8
PyMySQL==1.1.1
cryptography==43.0.0
Pillow==10.4.0
django-allauth==64.0.0
PyJWT[crypto]==2.9.0  # exigido pelo allauth p/ Sign in with Apple
requests==2.32.3
django-crispy-forms==2.3
crispy-bootstrap5==2024.10
django-widget-tweaks==1.5.0
whitenoise==6.7.0