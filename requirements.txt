# Django
Django==5.0.7
django-environ==0.11.2
python-decouple==3.8

# Banco de dados
psycopg[binary]==3.2.3  # Postgres serverless (Vercel/Neon/Supabase)
Pillow==10.4.0

# Autenticação social
django-allauth==64.0.0
PyJWT[crypto]==2.9.0  # exigido pelo allauth p/ Sign in with Apple (socialaccount)

# Pagamentos (HTTP client p/ gateways)
requests==2.32.3

# Frontend utilitários
django-crispy-forms==2.3
crispy-bootstrap5==2024.10
django-widget-tweaks==1.5.0

# Static files
whitenoise==6.7.0

# Dev / testes
coverage==7.15.4
ruff==0.5.6
pre-commit==3.8.0
