[tool.ruff]
line-length = 100
extend-exclude = ["migrations", "staticfiles", "static", "media", "venv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]

[tool.coverage.run]
source = ["apps"]
omit = ["apps/*/migrations/*"]

[tool.coverage.report]
show_missing = true
skip_covered = true
